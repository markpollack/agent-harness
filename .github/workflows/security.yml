name: Security Scan (quarantined OWASP)

# Historical OWASP dependency-check integration. Its NVD acquisition path is quarantined and this
# workflow is not release-clear evidence. Release security evidence comes from the separately
# validated, frozen Trivy database against fresh published-shape consumer closures.
on:
  workflow_dispatch:

jobs:
  scan:
    uses: markpollack/build-tools/.github/workflows/security-scan.yml@35297f1ade5f47c2925d6dab42a7e2d43bd734d0
    secrets: inherit
